import BookExperience from "../BookExperience";

export const metadata = {
  title: "Demo Book | Interactive Story Book",
  description: "Open the main interactive demo book.",
};

export default function DemoBookPage() {
  return <BookExperience />;
}
